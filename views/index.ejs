<html lang = "en">
<%- include('./partials/head.ejs') %>
<body>

    <%- include('./partials/nav.ejs') %>

    <div class="content"><h2>Your Current Courses</h2></div>
    <div class="classes">        
        <% if (classes.length > 0) { %>
            <% classes.forEach(entry => { %>

                <div class="entry">
                    <h3 class="title"><%= entry.title %></h3>
                </div>

            <% }) %>
        <% } else { %>
            <p>Blah blah blah</p>
        <% } %> 

    </div>

    <%- include('./partials/footer.ejs') %>

</body>
</html>